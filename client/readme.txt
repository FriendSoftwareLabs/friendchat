How to Hello

- rename example.local.config.js to local.config.js - replace the old file if
 it already exists ( check for any important values you might want to keep though ).

If you also want to run a Hello server:

- go to <place-with-serverfiles> and follow readme.txt there.